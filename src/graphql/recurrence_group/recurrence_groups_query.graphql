query recurrenceGroupsQuery($userId: ID!) {
  recurrenceGroups(userId: $userId) {
    id
    name
    recurrenceRules {
      id
      dayOfWeek
      endTime
      startTime
    }
    linkSubscriptions {
      id
      url
    }
  }
}
