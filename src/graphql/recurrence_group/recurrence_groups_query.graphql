query recurrenceGroupsQuery($userId: ID!) {
  recurrenceGroups(userId: $userId) {
    id
    name
    recurrenceRules {
      id
      endTime
      startTime
      beginDay
      endDay
    }
    linkSubscriptions {
      id
      url
    }
  }
}
